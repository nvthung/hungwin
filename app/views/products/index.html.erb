<<<<<<< HEAD
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Web Bán Quần Áo</title>

  <style>
    /* General Reset and Styling */
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f4f4f4;
    }
=======
<h1>Products</h1>
<h1><%= t ".title" %></h1>


<%= link_to "New product", new_product_path if authenticated? %>
<%= link_to "Login", new_session_path unless authenticated? %>

>>>>>>> f12c778 (help)

    h1 {
      text-align: center;
      padding: 20px;
      font-size: 36px;
      color: #333;
    }

    /* Navigation Menu */
    nav {
      background-color: #333;
      padding: 10px 20px;
      display: flex;
      justify-content: center;
      gap: 20px;
    }

    nav a {
      color: white;
      text-decoration: none;
      font-size: 18px;
      padding: 8px 15px;
      border-radius: 5px;
      background-color: #333;
    }

    nav a:hover {
      background-color: #575757;
    }

    /* Header Section */
    .header {
      background-image: url('https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcSv-nldz7uVoWRKjy-h52z9ZfmtsnrhPnKkWw&s');
      background-size: cover;
      background-position: center;
      height: 300px;
      color: white;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
    }

    .header h2 {
      font-size: 50px;
      font-weight: bold;
      text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.7);
    }

    /* Product Table */
    #products {
      width: 90%;
      margin: 20px auto;
      border-collapse: collapse;
    }

    #products th, #products td {
      border: 1px solid #ddd;
      padding: 15px;
      text-align: left;
    }

    #products th {
      background-color: #333;
      color: white;
    }

    #products tr:nth-child(even) {
      background-color: #f9f9f9;
    }

    .product-name {
      font-size: 18px;
      font-weight: bold;
    }

    .product-quantity {
      font-size: 16px;
      color: #888;
    }

    /* Product Image */
    .product-image img {
      width: 200px;        /* Set the width to 200px */
      height: 200px;       /* Set the height to 200px */
      object-fit: cover;   /* Ensures the image covers the 200x200 area without distortion */
      border-radius: 8px;  /* Optional: add border radius for rounded corners */
      margin: 0 auto;      /* Center the image horizontally */
    }

    /* Footer */
    footer {
      text-align: center;
      background-color: #333;
      color: white;
      padding: 20px;
    }
    /* Style for the "New product" link */
    /* Style for the "New product" link */
    .new-product-link {
      background-color: white; /* White background */
      color: black; /* Black text */
      font-size: 16px;
      padding: 10px 20px;
      text-decoration: none;
      border-radius: 5px;
      display: inline-block;
      transition: background-color 0.3s, color 0.3s;
      text-align: center;
      margin: 20px auto; /* Centers the link horizontally */
      display: block; /* This is required to allow centering */
    }
    
    /* Hover effect */
    .new-product-link:hover {
      background-color: blue; /* Blue background on hover */
      color: white; /* White text on hover */
    }
    


  </style>
</head>
<body>
  <!-- Navigation Bar -->
  

  <!-- Header Section with Background Image -->
  <div class="header">
    <h2>Web Bán Quần Áo</h2>
  </div>

  <!-- Products Section (Table Layout) -->
  <h1>Our Products</h1>
  <%= link_to "New product", new_product_path, class: "new-product-link" if authenticated? %>


  <table id="products">
    <thead>
      <tr>
        <th>Product Name</th>
        <th>Quantity</th>
      </tr>
    </thead>
    <tbody>
      <% @products.each do |product| %>
        <tr>
        <td class="product-name">
        <%= link_to product.name, product %>
        <!-- Product image -->
        <div class="product-image">
          <% if product.featured_image.attached? %>
            <img src="<%= url_for(product.featured_image) %>" alt="<%= product.name %>">
          <% else %>
            <img src="default-image-url.jpg" alt="No image available">
          <% end %>
        </div>
      </td>
      
        </tr>
      <% end %>
    </tbody>
  </table>

  <!-- Footer Section -->
  <footer>
    <p>&copy; 2025 Web Bán Quần Áo | All Rights Reserved</p>
  </footer>
</body>
</html>
